name: dolar-col-peso
description: Robot que obtiene el valor del dólar frente al peso colombiano

tasks:
  get_usd_cop:
    command:
      - python
      - main.py

artifactsDir: artifacts

condaConfigFile: conda.yaml

ignoreFiles:
  - .git
  - .vscode
  - __pycache__
